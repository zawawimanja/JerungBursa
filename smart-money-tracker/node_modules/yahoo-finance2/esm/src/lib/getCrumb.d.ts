import type { ExtendedCookieJar } from "./cookieJar.js";
import type { Logger } from "./options.js";
type Fetch = typeof fetch;
type CrumbOptions = RequestInit & {
    devel?: boolean | string;
};
export declare function _getCrumb(cookieJar: ExtendedCookieJar, fetch: Fetch, fetchOptionsBase: CrumbOptions, logger: Logger, url?: string, develOverride?: string, noCache?: boolean): Promise<string | null>;
export declare function getCrumbClear(cookieJar: ExtendedCookieJar): Promise<void>;
export default function getCrumb(cookieJar: ExtendedCookieJar, fetch: Fetch, fetchOptionsBase: CrumbOptions, logger: Logger, url?: string, __getCrumb?: typeof _getCrumb): Promise<string | null>;
export {};
//# sourceMappingURL=getCrumb.d.ts.map